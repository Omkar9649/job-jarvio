# Phase 3 — AI agent (not built yet)

Planned modules:

```
agent/
├── resume_parser.py    # read your resume (PDF/text)
├── embedder.py         # Ollama embeddings
├── matcher.py          # vector search + LLM rerank
└── notifier.py         # email / Telegram alerts
```

**Prerequisite:** Phase 0–2 done (jobs in MongoDB, n8n running daily scrape).

See [LEARNING.md](../LEARNING.md) Phase 3.
